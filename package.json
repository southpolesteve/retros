{
  "name": "retros",
  "version": "1.0.0",
  "description": "Real-time retrospective app for teams",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute retros-db --local --file=src/db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260107.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.57.0"
  }
}
